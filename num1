package main

import (
	"fmt"
)

func vis(s int) bool {
	if (s%4 == 0 && s%100 != 0) || (s%400 == 0) {
		return true
	}
	return false
}
func main() {
	var a int
	fmt.Scanln(&a)
	fmt.Println(vis(a))
}
